anthropic
python-dotenv
feedparser
requests
beautifulsoup4
edge-tts
gTTS
moviepy==1.0.3
imageio==2.31.1
imageio-ffmpeg
pillow
numpy
opencv-python
youtube-transcript-api
yt-dlp>=2026.08.19  # YouTube rompe compatibilidad a menudo; mantener actualizado (pip install --upgrade yt-dlp)
# API server
fastapi
uvicorn[standard]
python-multipart
# Hotspots / library
qrcode[pil]
# Music library
mutagen
librosa
# TTS opcional
elevenlabs
# YouTube Data API (subida automática de videos)
google-api-python-client
google-auth
google-auth-oauthlib
# Opcional: para transcribir videos locales con --video (requiere ffmpeg)
# openai-whisper
# Sincroniza la letra con el audio en el Montaje (word timestamps, CPU, sin torch).
# El modelo (~145 MB "base") se baja solo la primera vez a /root/.cache/huggingface
# (montado como volumen en docker-compose para que persista).
faster-whisper
click<8.2
